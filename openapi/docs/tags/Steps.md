Steps represent an action that one should perform in order to get recreate a recipe. Steps must belong to a recipe and they are versioned.

The Steps endpoints allows users retrieve and modify steps.
